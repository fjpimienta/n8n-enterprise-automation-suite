import { TimeOnIcePipe } from './time-on-ice-pipe';

describe('TimeOnIcePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeOnIcePipe();
    expect(pipe).toBeTruthy();
  });
});
